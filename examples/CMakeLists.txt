function(axes_add_example name deps)
  file(GLOB_RECURSE sources "${name}/*.cpp")
  add_executable(${name} ${sources})
  target_link_libraries(${name} PRIVATE ${deps})
  axes_set_warn_levels(${name})
endfunction(axes_add_example)

find_package(libigl CONFIG REQUIRED)

axes_add_example(empty axes)
axes_add_example(basic_graphics axes_gui)
axes_add_example(optim axes)
axes_add_example(poisson2d axes_gui)
axes_add_example(poisson3d axes)
axes_add_example(decimate axes_gui)
axes_add_example(lattice_poisson2d axes_gui)
axes_add_example(char_burgers axes_gui)
axes_add_example(surface_reconstruct axes_gui)
axes_add_example(normal axes_gui)
axes_add_example(arap_parm axes_gui)
axes_add_example(fluid axes_gui)
axes_add_example(fem_implicit axes_gui)
axes_add_example(fem3debug axes_gui)

